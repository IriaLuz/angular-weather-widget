export interface WeatherData {
  name: string;
  humidity: number;
  hasClouds: boolean;
  isDay: boolean;
  temp_celcius: string;
  temp_min: string;
  temp_max: string;
  temp_feels_like: string;
}
